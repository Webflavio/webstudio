# .nixpacks.toml

[phases.setup]

packages = ["nodejs_20", "pnpm-9_x", "openssl"]

[phases.install]

cmds = [
  "npm install -g corepack@0.24.1",
  "corepack enable",
  "pnpm i --frozen-lockfile"
]

[phases.build]

cmds = [
  "pnpm run build"
]

[phases.start]

cmd = "pnpm dev"
